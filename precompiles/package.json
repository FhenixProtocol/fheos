{
  "name": "build-precompiles",
  "description": "Small package containing the code used in our precompiles",
  "version": "1.0.0",
  "author": {
    "name": "FhenixProtocol",
    "url": "https://github.com/FhenixProtocol"
  },
  "devDependencies": {
    "hardhat": "^2.19.3",
    "ts-node": "10.9.2",
    "typescript": "4.9.5"
  },
  "files": [
    "contracts"
  ],
  "scripts": {
    "build": "hardhat compile",
    "clean": "rimraf ./artifacts ./cache ./coverage ./types ./coverage.json ./deployments/localfhenix && pnpm typechain"
  }
}
